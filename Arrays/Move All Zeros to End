
class Solution:
	def pushZerosToEnd(self,arr):
    	i=0
    	k=0
    	while i < len(arr):
    	    if arr[i]!=0:
    	        arr[i],arr[k]=arr[k],arr[i]
    	        i+=1
    	        k+=1
    	    else:
    	        i+=1
        return arr
